'use strict';

module.exports = function (app) {
    // var item = require('./controller/item');

    // app.route('/')
    //     .get(mahasiswa.index);

    // app.route('/mahasiswa/')
    //     .get(mahasiswa.get);

    // app.route('/mahasiswa/get')
    //     .get(mahasiswa.get);

    // app.route('/mahasiswa/get_by_id/')
    //     .get(mahasiswa.get_by_id);
        
    // app.route('/mahasiswa/add')
    //     .post(mahasiswa.add);

    // app.route('/mahasiswa/update')
    //     .put(mahasiswa.update);

    // app.route('/mahasiswa/delete')
    //     .delete(mahasiswa.delete);

    // app.route('/mahasiswa/get_matkul')
    //     .get(mahasiswa.get_matkul);
}